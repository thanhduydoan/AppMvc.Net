@using App.Models
@model List<ProductModel>
    
    <h1>Danh sach cac san pham</h1>

    <ul>
        @foreach (var product in Model)
        {
            <li>
                @product.Name
            </li>
        }
    </ul>
    <a asp-action=""></a>
